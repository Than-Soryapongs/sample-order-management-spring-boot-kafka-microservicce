<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sample.order_service.mapper.OrderMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (product_id, quantity, status)
        VALUES (#{productId}, #{quantity}, #{status})
    </insert>

    <select id="findById" resultType="com.sample.order_service.model.Order">
        SELECT id, product_id, quantity, status, created_at
        FROM orders
        WHERE id = #{id}
    </select>

</mapper>